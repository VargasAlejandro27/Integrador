<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard Admin - EcoCalc</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
:root {
    --primary-color: #16a34a;
    --primary-dark: #0f766e;
    --secondary-color: #2563eb;
    --danger-color: #dc2626;
    --warning-color: #f59e0b;
    --info-color: #3b82f6;
    --success-color: #22c55e;
    --bg-light: #f9fafb;
    --bg-lighter: #f3f4f6;
    --border-color: #e5e7eb;
    --text-primary: #1f2937;
    --text-secondary: #6b7280;
    --radius: 12px;
    --shadow-sm: 0 2px 6px rgba(0,0,0,0.08);
    --shadow-md: 0 4px 12px rgba(0,0,0,0.1);
    --shadow-lg: 0 8px 20px rgba(0,0,0,0.12);
}

/* Body */
body {
    font-family: 'Inter', 'Segoe UI', Roboto, sans-serif;
    background: var(--bg-light);
    color: var(--text-primary);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
}

/* Header */
.admin-header {
    background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
    color: #fff;
    padding: 40px 30px;
    margin-bottom: 40px;
    border-radius: 0 0 var(--radius) var(--radius);
    box-shadow: var(--shadow-lg);
    text-align: center;
}

.admin-header h1 {
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 8px;
}

.admin-header p {
    opacity: 0.85;
    font-size: 1rem;
}

/* Cards */
.stat-card {
    background: #fff;
    border-radius: var(--radius);
    padding: 28px;
    margin-bottom: 24px;
    box-shadow: var(--shadow-md);
    border-left: 5px solid var(--primary-color);
    transition: transform 0.25s ease, box-shadow 0.25s ease;
}

.stat-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
}

.stat-card.users { border-left-color: var(--info-color); }
.stat-card.calculations { border-left-color: var(--danger-color); }
.stat-card.emissions { border-left-color: var(--warning-color); }

.stat-icon {
    font-size: 34px;
    margin-right: 15px;
    opacity: 0.75;
}

.stat-value {
    font-size: 30px;
    font-weight: 700;
    color: var(--text-primary);
}

.stat-label {
    font-size: 0.9rem;
    color: var(--text-secondary);
    margin-top: 6px;
}

/* Section Titles */
.section-title {
    font-size: 1.4rem;
    font-weight: 700;
    color: var(--text-primary);
    margin-top: 35px;
    margin-bottom: 20px;
    border-bottom: 3px solid var(--primary-color);
    padding-bottom: 8px;
}

/* Tables */
.table-container {
    background: #fff;
    border-radius: var(--radius);
    padding: 25px;
    box-shadow: var(--shadow-md);
    margin-bottom: 35px;
    overflow-x: auto;
}

table {
    width: 100%;
    border-collapse: collapse;
}

thead {
    background: var(--bg-lighter);
    border-bottom: 2px solid var(--primary-color);
}

thead th {
    color: var(--text-primary);
    font-weight: 600;
    padding: 15px;
    text-transform: uppercase;
    font-size: 0.85rem;
    letter-spacing: 0.5px;
}

tbody td {
    padding: 12px 15px;
    border-bottom: 1px solid var(--border-color);
    color: var(--text-secondary);
}

tbody tr:hover {
    background: #f9fbfc;
}

/* Badges */
.badge {
    padding: 6px 14px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 0.5px;
}

.badge-admin { background-color: var(--danger-color); color: #fff; }
.badge-user { background-color: var(--info-color); color: #fff; }

/* Levels */
.level-badge {
    display: inline-block;
    padding: 6px 12px;
    border-radius: 15px;
    font-size: 12px;
    font-weight: 600;
    text-transform: uppercase;
}

.level-excelente { background: linear-gradient(135deg, #16a34a, #0f766e); color: #fff; }
.level-bueno { background: linear-gradient(135deg, #3b82f6, #1d4ed8); color: #fff; }
.level-promedio { background: linear-gradient(135deg, #f59e0b, #d97706); color: #fff; }
.level-alto { background: linear-gradient(135deg, #ef4444, #dc2626); color: #fff; }
.level-muy-alto { background: linear-gradient(135deg, #8b5cf6, #6d28d9); color: #fff; }

/* Navbar */
.navbar-admin {
    background: linear-gradient(135deg, var(--primary-color), var(--primary-dark));
    box-shadow: var(--shadow-md);
}

.navbar-admin .navbar-brand {
    color: #fff !important;
    font-weight: 700;
    font-size: 1.4rem;
}

.navbar-admin a {
    color: #fff !important;
    font-weight: 500;
}

.navbar-admin a:hover {
    opacity: 0.85;
}

/* Logout Button */
.logout-btn {
    background-color: var(--danger-color);
    color: #fff;
    border: none;
    padding: 8px 18px;
    border-radius: 6px;
    cursor: pointer;
    transition: background-color 0.25s ease;
    font-weight: 600;
}

.logout-btn:hover {
    background-color: #b91c1c;
}

/* Level Stats */
.level-stats {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
    gap: 18px;
    margin-top: 25px;
}

.level-stat {
    background: #fff;
    padding: 18px;
    border-radius: 10px;
    text-align: center;
    box-shadow: var(--shadow-sm);
}

.level-stat .count {
    font-size: 26px;
    font-weight: 700;
    color: var(--primary-color);
}

.level-stat .label {
    font-size: 13px;
    color: var(--text-secondary);
    margin-top: 6px;
    text-transform: capitalize;
}


    </style>
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark navbar-admin">
        <div class="container">
            <a class="navbar-brand" href="/admin/dashboard">
                <i class="bi bi-shield-check"></i> EcoCalc Admin
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <span class="nav-link">Bienvenido, <strong><%= user.name %></strong></span>
                    </li>
                    <li class="nav-item">
                        <a class="logout-btn" href="/admin/logout">
                            <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Admin Header -->
    <div class="admin-header">
        <div class="container">
            <h1><i class="bi bi-graph-up"></i> Panel de Administrador</h1>
            <p>Gestiona usuarios, cálculos y visualiza estadísticas del sistema</p>
        </div>
    </div>

    <!-- Contenido -->
    <div class="container">
        <!-- Estadísticas Globales -->
        <div class="row">
                <div class="stat-card users">
                    <div class="d-flex align-items-center">
                        <i class="bi bi-people stat-icon" style="color: #3498db;"></i>
                        <div>
                            <div class="stat-value"><%= stats.totalUsers %></div>
                            <div class="stat-label">Usuarios Totales</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="stat-card calculations">
                    <div class="d-flex align-items-center">
                        <i class="bi bi-calculator stat-icon" style="color: #e74c3c;"></i>
                        <div>
                            <div class="stat-value"><%= stats.totalCalculations %></div>
                            <div class="stat-label">Cálculos Realizados</div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="stat-card emissions">
                    <div class="d-flex align-items-center">
                        <i class="bi bi-graph-up stat-icon" style="color: #f39c12;"></i>
                        <div>
                            <div class="stat-value"><%= stats.averageEmissions %></div>
                            <div class="stat-label">Emisión Promedio (kg)</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Distribución por Niveles -->
        <div class="section-title">
            <i class="bi bi-pie-chart"></i> Distribución de Emisiones
        </div>
        <div class="level-stats">
            <% ['excelente', 'bueno', 'promedio', 'alto', 'muy-alto'].forEach(level => { %>
                <div class="level-stat">
                    <span class="level-badge level-<%= level %>"><%= level.toUpperCase() %></span>
                    <div class="count"><%= stats.byLevel[level] || 0 %></div>
                    <div class="label">usuarios</div>
                </div>
            <% }); %>
        </div>

        <!-- Lista de Usuarios -->
        <div class="section-title">
            <i class="bi bi-people"></i> Gestión de Usuarios
        </div>
        <div class="table-container">
            <% if (users.length > 0) { %>
                <div style="overflow-x: auto;">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nombre</th>
                                <th>Email</th>
                                <th>Rol</th>
                                <th>Fecha de Registro</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(user => { %>
                                <tr>
                                    <td><strong>#<%= user.id %></strong></td>
                                    <td><%= user.name %></td>
                                    <td><%= user.email %></td>
                                    <td>
                                        <select class="form-select form-select-sm role-select" data-user-id="<%= user.id %>" 
                                                style="max-width: 120px;">
                                            <option value="user" <%= user.role === 'user' ? 'selected' : '' %>>Usuario</option>
                                            <option value="admin" <%= user.role === 'admin' ? 'selected' : '' %>>Admin</option>
                                        </select>
                                    </td>
                                    <td><%= new Date(user.created_at).toLocaleDateString('es') %></td>
                                    <td>
                                        <button class="btn btn-sm btn-primary edit-user-btn" data-user-id="<%= user.id %>" 
                                                data-name="<%= user.name %>" data-email="<%= user.email %>">
                                            <i class="bi bi-pencil"></i>
                                        </button>
                                        <% if (user.id !== currentUserId) { %>
                                            <button class="btn btn-sm btn-danger delete-user-btn" data-user-id="<%= user.id %>">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <p class="text-muted">No hay usuarios registrados.</p>
            <% } %>
        </div>

        <!-- Últimos Cálculos -->
        <div class="section-title">
            <i class="bi bi-clock-history"></i> Últimos Cálculos
        </div>
        <div class="table-container">
            <% if (calculations.length > 0) { %>
                <div style="overflow-x: auto;">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Usuario</th>
                                <th>Email</th>
                                <th>Fecha</th>
                                <th>Emisiones (kg)</th>
                                <th>Nivel</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% calculations.forEach(calc => { %>
                                <tr>
                                    <td><strong><%= calc.user_name %></strong></td>
                                    <td><%= calc.email %></td>
                                    <td><%= new Date(calc.created_at).toLocaleDateString('es') %></td>
                                    <td><%= calc.total_emissions %> kg</td>
                                    <td>
                                        <span class="level-badge level-<%= calc.level %>">
                                            <%= calc.level.charAt(0).toUpperCase() + calc.level.slice(1) %>
                                        </span>
                                    </td>
                                    <td>
                                        <button class="btn btn-sm btn-danger delete-calc-btn" data-calc-id="<%= calc.id %>">
                                            <i class="bi bi-trash"></i> Eliminar
                                        </button>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <p class="text-muted">No hay cálculos registrados.</p>
            <% } %>
        </div>
    </div>

    <footer class="text-center" style="padding: 30px; color: #7f8c8d; margin-top: 30px;">
        <p>&copy; 2026 EcoCalc - Panel de Administrador</p>
    </footer>

    <!-- Modal para editar usuario -->
    <div class="modal fade" id="editUserModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Editar Usuario</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <form id="editUserForm">
                        <input type="hidden" id="editUserId">
                        <div class="mb-3">
                            <label class="form-label">Nombre</label>
                            <input type="text" class="form-control" id="editUserName" required>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Email</label>
                            <input type="email" class="form-control" id="editUserEmail" required>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="button" class="btn btn-primary" id="saveEditUserBtn">Guardar Cambios</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Editar usuario
        document.querySelectorAll('.edit-user-btn').forEach(btn => {
            btn.addEventListener('click', function() {
                document.getElementById('editUserId').value = this.dataset.userId;
                document.getElementById('editUserName').value = this.dataset.name;
                document.getElementById('editUserEmail').value = this.dataset.email;
                new bootstrap.Modal(document.getElementById('editUserModal')).show();
            });
        });

        document.getElementById('saveEditUserBtn').addEventListener('click', async function() {
            const userId = document.getElementById('editUserId').value;
            const name = document.getElementById('editUserName').value;
            const email = document.getElementById('editUserEmail').value;

            try {
                const response = await fetch(`/admin/api/users/${userId}`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ name, email })
                });

                if (response.ok) {
                    alert('✅ Usuario actualizado');
                    location.reload();
                } else {
                    alert('❌ Error al actualizar usuario');
                }
            } catch (err) {
                alert('❌ Error: ' + err.message);
            }
        });

        // Cambiar rol
        document.querySelectorAll('.role-select').forEach(select => {
            const originalValue = select.value;
            select.addEventListener('change', async function() {
                const userId = this.dataset.userId;
                const newRole = this.value;

                if (!confirm(`¿Cambiar rol de este usuario a ${newRole === 'admin' ? 'ADMINISTRADOR' : 'USUARIO'}?`)) {
                    this.value = originalValue;
                    return;
                }

                try {
                    const response = await fetch(`/admin/api/users/${userId}/role`, {
                        method: 'PUT',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ role: newRole })
                    });

                    if (response.ok) {
                        alert('✅ Rol actualizado');
                        location.reload();
                    } else {
                        alert('❌ Error al actualizar rol');
                        this.value = originalValue;
                    }
                } catch (err) {
                    alert('❌ Error: ' + err.message);
                    this.value = originalValue;
                }
            });
        });

        // Eliminar usuario
        document.querySelectorAll('.delete-user-btn').forEach(btn => {
            btn.addEventListener('click', async function() {
                if (!confirm('⚠️ ¿Eliminar este usuario y todos sus cálculos?')) {
                    return;
                }

                try {
                    const response = await fetch(`/admin/api/users/${this.dataset.userId}`, {
                        method: 'DELETE'
                    });

                    if (response.ok) {
                        alert('✅ Usuario eliminado');
                        location.reload();
                    } else {
                        alert('❌ Error al eliminar usuario');
                    }
                } catch (err) {
                    alert('❌ Error: ' + err.message);
                }
            });
        });

        // Eliminar cálculo
        document.querySelectorAll('.delete-calc-btn').forEach(btn => {
            btn.addEventListener('click', async function() {
                if (!confirm('⚠️ ¿Eliminar este cálculo?')) {
                    return;
                }

                try {
                    const response = await fetch(`/admin/api/calculations/${this.dataset.calcId}`, {
                        method: 'DELETE'
                    });

                    if (response.ok) {
                        alert('✅ Cálculo eliminado');
                        location.reload();
                    } else {
                        alert('❌ Error al eliminar cálculo');
                    }
                } catch (err) {
                    alert('❌ Error: ' + err.message);
                }
            });
        });
    </script>
</body>
</html>
